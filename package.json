{
  "name": "messaging-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "emulate": "cd functions && npm run build && cd .. && firebase emulators:start --only functions",
    "deploy": "cd functions && npm run build && cd .. && firebase deploy --only functions"
  }
}